 Django
- Тестирование с pytest

## 📋 API Endpoints

### Вопросы (Q# Q&A Service API

API сервис для вопросов и ответов, построенный на Django REST Framework с PostgreSQL.

## 🚀 Функциональность

- Создание, получение и удаление вопросов
- Добавление, получение и удаление ответов
- Каскадное удаление ответов при удалении вопроса
- Валидация данных
- Логирование операций
- Автоматическая документация API через drf-spectacular
- Админ-панель Django
- Тестирование с pytest

## 📋 API Endpoints

### Вопросы (Questions)
- `GET /api/questions/` — список всех вопросов
- `POST /api/questions/` — создать новый вопрос
- `GET /api/questions/{id}/` — получить вопрос и все ответы на него
- `DELETE /api/questions/{id}/` — удалить вопрос (вместе с ответами)

### Ответы (Answers)
- `POST /api/questions/{id}/answers/` — добавить ответ к вопросу
- `GET /api/answers/{id}/` — получить конкретный ответ
- `DELETE /api/answers/{id}/` — удалить ответ

## 🛠 Технологии

- Django 4.2
- Django REST Framework
- PostgreSQL
- Poetry (управление зависимостями)
- drf-spectacular (документация)
- pytest (тестирование)
- Docker & Docker Compose

## 🏃‍♂️ Быстрый старт

### Локальная разработка с Poetry

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd problems_service
```

2. **Установите зависимости:**
```bash
poetry install
```

3. **Создайте .env в рабочей дирректории:**
```
SECRET_KEY=здесь ваш ключ
ALLOWED_HOSTS=localhost
DEBUG=True

DB_NAME=название базы
PG_USER=пользователь
PG_PASSWORD=пароль
DB_HOST=хост для запуска
DB_PORT=порт
```

4. **Активируйте виртуальное окружение:**
```bash
poetry shell
```

5. **Настройте базу данных:**
```bash
python manage.py seed_data
```

6. **Заполните БД тестовыми данными:**
```bash
python manage.py seed_data
```

7. **Создайте суперпользователя:**
```bash
python manage.py createsuperuser
```

8. **Запустите сервер:**
```bash
python manage.py runserver
```

## Запуск с Docker

1. **Соберите и запустите контейнеры:**
```bash
docker-compose up --build
```

2. **Приложение будет доступно по адресу:**

[http://localhost:8000](http://localhost:8000)


## 🧪 Тестирование
1. **Запуск тестов pytest:**
```bash
poetry run pytest
```
2. **Запуск проверки типизации:**
```bash
poetry run mypy apps/
```


## 📚 Документация API

После запуска приложения документация доступна по адресам:

- Swagger UI: http://localhost:8000/docs/

- ReDoc: http://localhost:8000/redoc/

- Схема OpenAPI: http://localhost:8000/api/schema/